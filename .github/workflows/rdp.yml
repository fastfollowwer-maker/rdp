name: Ultimate-1D-Counter-No Script

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022
    steps:
      
      # -----------------------------------------------------------
      # خطوة 1: تثبيت المتطلبات الأساسية (Chrome, CRD, WARP)
      # -----------------------------------------------------------
      - name: Install Prerequisites (Chrome, CRD, WARP)
        shell: pwsh
        run: |
          Write-Host "⬇️ Downloading Chrome..."
          Invoke-WebRequest -Uri "https://dl.google.com/chrome/install/latest/chrome_installer.exe" -OutFile "chrome_installer.exe"
          Start-Process -FilePath "chrome_installer.exe" -Args "/silent /install" -Wait
          
          Write-Host "⬇️ Downloading CRD Host..."
          Invoke-WebRequest -Uri "https://dl.google.com/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi" -OutFile "crd.msi"
          Start-Process -FilePath "msiexec.exe" -Args "/i crd.msi /quiet /qn" -Wait

          Write-Host "⬇️ Downloading Cloudflare WARP (لتغيير الـ IP إلى شبكة منزلية ظاهرياً)..."
          Invoke-WebRequest -Uri "https://1111-releases.cloudflareclient.com/windows/Cloudflare_WARP_Release-x64.msi" -OutFile "warp.msi"
          Start-Process -FilePath "msiexec.exe" -Args "/i warp.msi /quiet /qn" -Wait

      # -----------------------------------------------------------
      # خطوة 2: تحسين بصمة نظام التشغيل (بدون إعادة تشغيل)
      # -----------------------------------------------------------
      - name: Apply System Fingerprint Evasion (No Reboot)
        shell: pwsh
        run: |
          Write-Host "🛡️ إخفاء علامات الـ VPS/RDP (بدون إعادة تشغيل)..."

          # 1. تغيير اسم الجهاز (سيبقى مؤقتاً حتى إعادة التشغيل، لكنه يُسجّل في السجل)
          $NewName = "DESKTOP-$(Get-Random -Maximum 9999)"
          Rename-Computer -NewName $NewName -Force
          Write-Host "✅ تم تغيير اسم الجهاز إلى: $NewName (سيفعل بعد إعادة التشغيل)"

          # 2. تعطيل خدمات Hyper-V فقط إذا كانت موجودة (لتجنب الخطأ)
          $HyperVPath = "HKLM:\SYSTEM\CurrentControlSet\Services\Hyper-V"
          if (Test-Path $HyperVPath) {
            Set-ItemProperty -Path $HyperVPath -Name "Start" -Value 4 -Type DWord -Force
            Write-Host "✅ Hyper-V تم تعطيله."
          } else {
            Write-Host "ℹ️ Hyper-V غير مثبت، تخطي."
          }

          # 3. تعطيل خدمات Virtualization الظاهرة الأخرى (إن وجدت)
          $services = @("vmms", "vmicheartbeat", "vmicexchange", "vmicguestinterface", "vmicshutdown", "vmictimesync")
          foreach ($svc in $services) {
            $svcPath = "HKLM:\SYSTEM\CurrentControlSet\Services\$svc"
            if (Test-Path $svcPath) {
              Set-ItemProperty -Path $svcPath -Name "Start" -Value 4 -Type DWord -Force
              Write-Host "✅ $svc تم تعطيله."
            }
          }

          # 4. تعديل TTL (Time To Live) ليبدو كجهاز Windows حقيقي (يُطبق فوراً عبر netsh)
          #    القيمة الافتراضية لـ Windows هي 128
          netsh int ipv4 set global defaultcurhoplimit=128
          Write-Host "✅ TTL تم ضبطه إلى 128."

          # 5. تعديل حجم النافذة (Window Scaling) وتحسينات TCP
          netsh int tcp set global autotuninglevel=normal
          netsh int tcp set global chimney=disabled
          netsh int tcp set global rss=enabled
          Write-Host "✅ إعدادات TCP تم تحسينها."

          # 6. إخفاء اسم المعالج (لتجنب كشف Xeon/VPS)
          $cpuPath = "HKLM:\HARDWARE\DESCRIPTION\System\CentralProcessor\0"
          if (Test-Path $cpuPath) {
            Set-ItemProperty -Path $cpuPath -Name "ProcessorNameString" -Value "Intel(R) Core(TM) i7-8700K CPU @ 3.70GHz" -Force
            Write-Host "✅ اسم المعالج تم تزييفه."
          }

          # 7. إضافة بصمة قرص صلب (لتبدو كقرص HDD وليس SSD سحابي)
          #    هذا مجرد تزييف، لا يؤثر فعلياً لكن قد يساعد في بعض الكشوفات
          Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\Memory Management" -Name "ClearPageFileAtShutdown" -Value 1 -Type DWord -Force

          Write-Host "✅ تم إخفاء بصمات الـ VPS بنجاح (بدون إعادة تشغيل)!"

      # -----------------------------------------------------------
      # خطوة 3: تشغيل Cloudflare WARP (لتغيير IP المصدر)
      # -----------------------------------------------------------
      - name: Connect to Cloudflare WARP (إخفاء IP المصدر)
        shell: pwsh
        run: |
          Write-Host "⏳ انتظار تثبيت WARP..."
          Start-Sleep -Seconds 30

          $WarpCli = "C:\Program Files\Cloudflare\Cloudflare WARP\warp-cli.exe"
          
          Write-Host "🚀 تسجيل الجهاز في WARP..."
          & $WarpCli registration new
          
          Write-Host "🔗 الاتصال بشبكة WARP..."
          & $WarpCli connect
          
          Write-Host "📡 جلب الـ IP الجديد (بعد WARP)..."
          $NewIP = (Invoke-WebRequest -Uri "https://api.ipify.org" -UseBasicParsing).Content
          Write-Host "✅ الـ IP الحالي (بعد التخفي): $NewIP"
          
          Write-Host "🧪 التحقق من نوع الـ IP..."
          $ipInfo = Invoke-RestMethod -Uri "https://ipinfo.io/$NewIP/json"
          Write-Host "📌 معلومات الـ IP: $($ipInfo | ConvertTo-Json)"

      # -----------------------------------------------------------
      # خطوة 4: تفعيل الريموت ديسكتوب (Chrome Remote Desktop)
      # -----------------------------------------------------------
      - name: Start Chrome Remote Desktop
        shell: pwsh
        env:
          CRD_CODE: ${{ secrets.CRD_CODE }}
          CRD_PIN: ${{ secrets.CRD_PIN }}
        run: |
          Start-Sleep -Seconds 15
          
          $exePath = "${Env:PROGRAMFILES(X86)}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe"
          $UniqueName = "${{ github.actor }}-Machine-${{ env.DEVICE_NUM }}"
          
          Write-Host "🚀 STARTING RDP SERVICE: $UniqueName"
          & $exePath --code="$env:CRD_CODE" --redirect-url="https://remotedesktop.google.com/_/oauthredirect" --name="$UniqueName" --pin=$env:CRD_PIN

      # -----------------------------------------------------------
      # خطوة 5: تجهيز المتصفح للتمويه الكامل (Chrome Stealth)
      # -----------------------------------------------------------
      - name: Setup Chrome for Stealth (Anti-Detection)
        shell: pwsh
        run: |
          Write-Host "🛠️ تجهيز Chrome بكوكيز وإضافات لتجنب الكشف..."
          
          $UserDataDir = "C:\chrome-profile"
          New-Item -ItemType Directory -Path $UserDataDir -Force
          
          $ChromePath = "C:\Program Files\Google\Chrome\Application\chrome.exe"
          $ShortcutPath = "C:\Users\runneradmin\Desktop\Chrome-Stealth.lnk"
          
          $WScriptShell = New-Object -ComObject WScript.Shell
          $Shortcut = $WScriptShell.CreateShortcut($ShortcutPath)
          $Shortcut.TargetPath = $ChromePath
          $Shortcut.Arguments = "--user-data-dir=""$UserDataDir"" --disable-blink-features=AutomationControlled --disable-features=ChromeWhatsNewUI --disable-web-security --disable-gpu --no-sandbox --disable-dev-shm-usage --start-maximized"
          $Shortcut.Save()
          
          Write-Host "✅ تم إنشاء اختصار Chrome في وضع التخفي على سطح المكتب!"

      # -----------------------------------------------------------
      # خطوة 6: البقاء نشطاً (Keep Alive)
      # -----------------------------------------------------------
      - name: Keep Alive
        shell: pwsh
        run: |
          Write-Host "✅ الجهاز جاهز وبصمة مخفية تماماً (مع WARP وبدون إعادة تشغيل)."
          Write-Host "📌 تذكر: بعض التغييرات (مثل اسم الجهاز) ستفعل بعد إعادة تشغيل مستقبلية، ولكن الـ IP الحالي مخفى بـ WARP."
          while ($true) { 
            Start-Sleep -Seconds 300
            $WarpStatus = & "C:\Program Files\Cloudflare\Cloudflare WARP\warp-cli.exe" status
            Write-Host "[STATUS] $WarpStatus"
          }
